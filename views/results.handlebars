<h1>BEST MATCHING PETS</h1>
<div class="text-center">
    <h2>{{name}}</h2>
    <p>{{age}}</p>
    <p>{{Breed}}</p>
    <p>{{gender}}</p>
    <p>{{size}}</p>
</div>

<a href="/homepage">Go to homepage</a>
 
 <section class="container"></section>
  {{#if loggedIn}}
  <section class="flex-row justify-center">
    {{#each pet as |pet|}}
    {{> pet-details}}
    {{/each}}
  </section>
</section>
<!--link to login -->
{{else}}
<a href="/login">You must login first to view this page</a>
{{/if}}